R"##(

(define (standard boolean)
  (environment ()

    #;(define boolean?
      (lambda (x)
        (if (or (eq? x #true)
                (eq? x #false))
          #true
          #false)))

    (define boolean?
      (lambda (x)
        (if (eq? x #true)
            #true
            (eq? x #false))))

    (define not
      (lambda (x)
        (if x #false #true)))
    )
  )

)##"

