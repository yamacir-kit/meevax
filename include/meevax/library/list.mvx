R"##(

(define (standard list)
  (environment ()
    (define list
      (lambda x x))

    (define list-tail
      (lambda (x k)
        (if (zero? k) x
            (list-tail (cdr x) (- k 1)))))

    (define list-ref
      (lambda (x k)
        (car (list-tail x k))))
    )
  )

)##"

